node_modules/

# Runtime state — never commit build output, uploads, the database, logs,
# or backups (snapshots include the database and .env, both with secrets).
data/
builds/
uploads/
jobs/
logs/
backups/

# Secrets
.env
.env.local
*.pem
*.key

# Per-deployment web UI config (not a secret, but a server-specific value
# that must never be a tracked-file hand-edit — see web/config.example.js
# and scripts/setup.mjs, which generates this automatically).
web/config.js

# Editor/OS
.DS_Store
Thumbs.db
